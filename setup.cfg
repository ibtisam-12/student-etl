[metadata]
name = student-etl
version = 1.0.0
description = CSV to MongoDB ETL pipeline for student enrollment data
license = MIT

[options]
packages = find:
install_requires =
    pymongo>=4.0
    python-decouple>=3.8
